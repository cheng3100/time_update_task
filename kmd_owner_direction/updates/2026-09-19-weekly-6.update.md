# 2026-09-19 Weekly #6 — Curated Update

## 本期判断
七个 Owner taxonomy 不变。本期重点不是增加方向，而是把三个正确性边界做成正式 contract：VM fault capability、reset reconciliation、FW shared-memory ownership。

## Top features
1. **Memory P0 — Fault Capability Contract**：VM/context 显式记录 fault-mode、scratch policy、replay capability、PASID binding；fault handler 先 validation 再 HMM/migration/replay。
2. **RAS/Firmware P0/P1 — Reset Reconciliation Contract**：对 ring/fence/doorbell/VM/FW request/software shadow 定义 reset 后 reinit/rebase/stale/restore 行为。
3. **Virtualization P1 — VF Resource Capability Matrix**：present/initialized/assigned/active/resettable/migratable 分离。
4. **Memory P1 — VRAM Pressure + Reclaim/Eviction**：延续 Weekly #5，机制与 policy 分层。
5. **Firmware P1 — FW Shared-Memory Mapping Object**：统一 CPU mapping、device VA、bounds、coherency 和 FW generation。

## Industry signals
- Memory：Xe non-fault-mode scratch VM fault rejection；AMDGPU mapping-info ioctl cleanup 强调 VM introspection ownership/unwind。
- Virtualization：AMDGPU SR-IOV VF UVD resume 修复说明 VF resource initialization 与 PF 不对称。
- Power：本期无方向级高价值新增；firmware/VBIOS PM table validation 作为实现纪律跟踪。
- RAS：AMDGPU KIQ MODE1 reset 后 fence sequence reconciliation；multi-XCC hang bookkeeping 初始化维度修复。
- Multi-GPU：本期无方向级高价值新增；保留 topology cardinality review discipline。
- Observability：本期无新的 common-layer 方向变化；把 reconcile result 纳入 stable event schema。
- Firmware：Tyr MappedBo 将 KernelBo 与 persistent CPU vmap 绑定到同一 lifetime，并为 FW-reported VA bounds validation 建立 owner boundary。

## Detailed focus — Reset Reconciliation Contract
统一阶段：`QUIESCE → SNAPSHOT → RESET → GENERATION_ADVANCE → RECONCILE_LOW_LEVEL → FW_REHANDSHAKE → RESTORE_GLOBAL → RESTORE/FAIL_CONTEXT → VALIDATE → REOPEN`。

每个 reset-sensitive object 维护：owner、reset scope、HW/memory survival、SW shadow action、FW action、old-completion policy、post-reset validation。

## Leader Memory
本期深挖 `VM Fault Capability × Reset Generation`：fault worker 在 HMM walk 前验证 faultability、VA region、PASID/VMID/context generation、recovery generation；`mmu_notifier_seq / vm_generation / context_generation / recovery_generation` 保持独立语义。

## Durable resources
- Firmware/shared-memory：Tyr `MappedBo` patch series（Implementation / Practice）。
- Virtualization/Security：AMDGPU Process Isolation documentation（Canonical / implementation boundary）。
