# 2026-08-08 Test #3 — Structured Update

## Update-model changes validated
- Complete chat report remains mandatory; GitHub is additional persistence.
- Industry Updates are grouped by top-level Owner and refreshed every run.
- Learning Resources are stable/cumulative and organized by Owner → sub-direction.
- Stable taxonomy/summaries remain unchanged.
- Root-level cross-task rules now live in [`../UPDATE_RULES.md`](../../UPDATE_RULES.md).

## Current feature priorities
1. Memory — recoverable fault/HMM/migration/replay; preserve migration-unit abstraction.
2. Firmware — versioned async ABI + capability negotiation + restart/state reconciliation.
3. RAS — frozen snapshot/devcoredump + FW logs + reset reason.
4. Multi-GPU — stable identity/topology/P2P capability model.
5. Observability — stable event/object identity + eBPF tracing; PMU joins later.
6. Conditional — SR-IOV provisioning if hardware supports it; otherwise reusable PM telemetry.

## Industry update summary
### Memory
- GPU SVM/drm_pagemap remains the key upstream common-layer signal.
- `next` docs describe N:1 per-device `drm_gpusvm_pages`, relevant to future multi-GPU/shared-VA metadata.

### Virtualization / Security
- Xe configfs now documents pre-probe SR-IOV `max_vfs`/PF-mode configuration.
- Nova continues to validate shared lower HW/FW services for DRM and VFIO/vGPU clients.

### Power / Performance
- No high-value direction-changing item in the short Test #2→#3 window.
- Measurement/lifetime/constraint layer before complex governor remains the recommendation.

### Reliability / RAS
- No new direction-changing item in the short window.
- Snapshot-before-reset and FW-log retention remain the two strongest architectural invariants.

### Multi-GPU / Fabric
- GPU SVM N:1 per-device page-state model is a useful multi-device design clue.
- `fabric_ext` remains a long-term signal for movement/ordering/ownership/observability semantics.

### Observability / Profiling
- No new direction-changing item after Test #2.
- ProfInfer + gpu_ext/fabric_ext continue to support one trace/PMU object/timestamp model.

### Firmware / Control Plane
- Nova guidelines explicitly require firmware-version-independent APIs for second-level drivers.
- Nova task list keeps GSP-RM API instability and GSP log export visible as first-class problems.

## Resource library
Stable libraries now live under `../resources/` and are separate from news:
- Memory
- Virtualization/Security
- Power/Performance
- Reliability/RAS
- Multi-GPU/Fabric
- Observability/Profiling
- Firmware/Control Plane

Each stable sub-direction has at least one durable Learning Resource with a short reading note.

## Detailed feature of this run
**Stable GPU Event/Object Identity**

3–6 months:
- define device/client/PID/PASID/VM/context/queue/job/fault/migration/FW/reset identifiers;
- stable tracepoints for lifetime edges;
- one correlation tool.

1–2 years:
- join FW timestamp domains;
- join PMU samples and per-context attribution;
- use same IDs in crash snapshots;
- extend to multi-GPU/fabric timelines.

## Leader Memory deep dive
Study N:1 GPU SVM representation: one CPU VA/range with per-device page/mapping state. Separate CPU range identity, device residency, DMA mapping and GPU PTE ownership so future multi-GPU UVM does not require redesign.
